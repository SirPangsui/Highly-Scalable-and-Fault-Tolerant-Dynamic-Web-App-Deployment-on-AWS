provider "aws" {
  region  = var.region
  profile = "bryan-mike"

  default_tags {
    tags = {
      "Automation"  = "terraform"
      "Project"     = var.project_name
      "Environment" = var.environment
    }
  }
}